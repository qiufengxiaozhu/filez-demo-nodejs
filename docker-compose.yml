version: '3.8'

services:
  filez-demo-nodejs:
    build:
      context: .
      dockerfile: Dockerfile
    image: filez-demo-nodejs
    container_name: filez-demo-nodejs
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - PORT=${PORT:-8000}
    volumes:
      - filez-data:/app/data
      - filez-uploads:/app/uploads
    restart: unless-stopped

volumes:
  filez-data:
  filez-uploads:

